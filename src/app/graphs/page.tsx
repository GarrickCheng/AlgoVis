import GraphsVisualizer from "@/components/graphs/GraphsVisualizer";

export const metadata = {
  title: "Trees & Graphs — AlgoVis",
  description: "Visualize binary search trees and graph traversal algorithms: BFS, DFS, Dijkstra, Prim's MST.",
};

export default function GraphsPage() {
  return <GraphsVisualizer />;
}
